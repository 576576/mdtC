import modules/example
::todo:实现import from as
::import

::repeat和import使用示例
u.fx=u.f0+u.now
	u.now=u.now%u.max
	jump2(+(u.now*12+2))
repeat(u,3){
	ubind_s(@poly,1919810,u)
}
::bind.end
	u.now=(u.now+1)%u.max

::pass